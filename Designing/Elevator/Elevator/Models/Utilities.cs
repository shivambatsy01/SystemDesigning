namespace Elevator.Models
{
    public enum Dir
    {
        Up,
        Down
    }

    public enum CartState
    {
        Moving,
        Stopped
    }

    public enum ButtonState
    {
        Pressed,
        Unpressed
    }
}